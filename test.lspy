(load "prelude.lspy")

(print {hello})
(print {my_name_is_dan})


(fun {stuff _} {do
  (= {x} 100)
  (= {y} 50)
  (= {result} (+ x y))
  (result)
})

(print (+ (stuff ()) 20) (+ (stuff ()) 500))
